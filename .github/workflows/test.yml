name: Run Python Tests
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    environment: studi-crypto-tracker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Python 3
        uses: actions/setup-python@v1
        with:
          python-version: 3.9.12
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name : Run pyenv
        run: . env/bin/activate
        
      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1.3
        with:
          envkey_FLASK_ENV: development
          envkey_FLASK_APP: run
          envkey_DB_HOST: wb39lt71kvkgdmw0.cbetxkdyhwsb.us-east-1.rds.amazonaws.com
          envkey_DB_USERNAME: ${{ secrets.DB_USERNAME }} #prtwpb7v8hv3ti8u
          envkey_DB_PASSWORD: d6e8edsvxa2bju44
          envkey_DB_DATABASE: atsp62l7dqdqz9ve
          envkey_API_KEY_COIN: 3144c6fc-815d-46d1-a588-60bc4e062a5c
          file_name: .env
          
      - name : installation de pytest
        run: python -m pip install pytest pytest-dotenv pytest-flask
        
      - name : run pytest
        run: pytest -p no:warnings 
    
